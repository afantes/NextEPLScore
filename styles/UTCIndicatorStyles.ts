import styled from 'styled-components';

export const UTCIndicator = styled.div`
    position: fixed;
    bottom: 10px;
    right: 10px;
    background-color: rgba(0, 0, 0, 0.6);
    color: #fff;
    padding: 8px;
    border-radius: 3px;
    font-size: 0.9em;
`;
